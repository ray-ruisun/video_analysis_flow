# ============================================================================
# 核心依赖 (必需)
# ============================================================================
numpy>=1.23.0
opencv-python>=4.7.0
pillow>=9.0.0
python-docx>=0.8.11
loguru>=0.7.0
tqdm>=4.65.0
pyyaml>=6.0
colorama>=0.4.6

# ============================================================================
# 视觉分析模块 (visual) - 必需
# ============================================================================
scenedetect[opencv]>=0.6.0
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0

# ============================================================================
# YOLO 检测模块 (yolo) - 使用 yolo 模块时必需
# ============================================================================
ultralytics>=8.0.0

# ============================================================================
# 音频分析模块 (audio) - 使用 audio 模块时必需
# ============================================================================
librosa>=0.9.0
soundfile>=0.11.0
# essentia-tensorflow 在某些 Python 版本上有兼容性问题
# 如果安装失败，可以尝试: pip install essentia
# 或者跳过，audio 模块会有部分功能受限 (BGM风格、情绪分析)
essentia-tensorflow

# ============================================================================
# ASR 语音识别模块 (asr) - 使用 asr 模块时必需
# ============================================================================
faster-whisper>=0.9.0
torchaudio>=2.0.0
speechbrain>=0.5.14

# ASR 韵律分析 (可选，需要 enable_prosody=True)
parselmouth>=0.4.0

# ============================================================================
# 可选依赖
# ============================================================================
# transformers>=4.35.0      # 如果需要额外的 NLP 功能
# openai-whisper>=20231117  # faster-whisper 的替代方案
# requests>=2.31.0          # 网络请求 (当前未使用)