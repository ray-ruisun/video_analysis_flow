# ============================================================================
# Video Style Analysis Pipeline - Dependencies
# 纯 PyTorch + HuggingFace 方案，避免 TensorFlow 依赖
# ============================================================================

# ============================================================================
# 核心依赖 (必需)
# ============================================================================
numpy>=1.23.0
opencv-python>=4.7.0
pillow>=9.0.0
python-docx>=0.8.11
loguru>=0.7.0
tqdm>=4.65.0
pyyaml>=6.0
colorama>=0.4.6

# ============================================================================
# PyTorch 生态系统 (核心)
# ============================================================================
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ============================================================================
# HuggingFace Transformers (音频分类、情感分析)
# ============================================================================
transformers>=4.35.0
# 模型会自动从 HuggingFace Hub 下载:
# - MIT/ast-finetuned-audioset-10-10-0.4593 (音频分类)
# - ehcalabres/wav2vec2-lg-xlsr-en-speech-emotion-recognition (情感分析)

# ============================================================================
# 视觉分析模块 (visual)
# ============================================================================
scenedetect[opencv]>=0.6.0
timm>=0.9.0
# 场景分类使用 Places365 ResNet-18 (自动下载)

# ============================================================================
# YOLO 检测模块 (yolo)
# ============================================================================
ultralytics>=8.0.0

# ============================================================================
# 音频分析模块 (audio)
# ============================================================================
librosa>=0.9.0
soundfile>=0.11.0
# 不再需要 essentia-tensorflow!
# 使用 librosa + HuggingFace transformers 替代

# ============================================================================
# ASR 语音识别模块 (asr)
# ============================================================================
faster-whisper>=0.9.0
# 不再需要 speechbrain!
# 使用 HuggingFace transformers 替代情感分析

# ============================================================================
# 可选依赖
# ============================================================================
# openai-whisper>=20231117  # faster-whisper 的替代方案
# requests>=2.31.0          # 网络请求 (当前仅用于下载模型)
